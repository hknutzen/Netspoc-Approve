#!/bin/sh
# Wrapper script for approve.pl
# Calls approve.pl via "sudo" as "systemuser".

USER=$(get-netspoc-approve-conf systemuser)
CMD=/usr/local/bin/approve.pl
[ -z "$USER" ] || [ $(id -un) = $USER ] || CMD="sudo -u $USER $CMD"
exec $CMD $@
